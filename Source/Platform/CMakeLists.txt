WEBKIT_INCLUDE_CONFIG_FILES_IF_EXISTS()

include_directories()
ADD_LIBRARY(${Platform_LIBRARY_NAME} STATIC ${Platform_SOURCES})
SET_TARGET_PROPERTIES(${Platform_LIBRARY_NAME} PROPERTIES FOLDER "Platform")
